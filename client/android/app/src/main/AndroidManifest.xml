<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- WP-M6 (F10-22): `bebok://pair?v=1&ep=...&code=...&fp=...` deep
                 link - the scan-free way into pairing (tap on the desktop's
                 pairing card, WP-M8's e2e hook via `am start -d`). Delivered
                 to the JS side by Capacitor's App plugin (`appUrlOpen`, or
                 `getLaunchUrl()` on a cold start); `launchMode="singleTask"`
                 above keeps one activity, so a link while running arrives via
                 onNewIntent. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bebok" android:host="pair" />
            </intent-filter>

        </activity>

        <!-- WP-M6 (F10-22): ML Kit downloads the barcode model on install
             instead of on first scan (`@capacitor-mlkit/barcode-scanning`). -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <!-- F10-10: raised for the duration of a turn by
             EngineLauncherPlugin.beginWork()/endWork(). -->
        <service
            android:name=".EngineForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- WP-M6 (F10-27): package visibility for "Open Tailscale" (the
         AppLauncher plugin resolves the launch intent by package name, which
         Android 11+ hides without an explicit <queries> entry). -->
    <queries>
        <package android:name="com.tailscale.ipn" />
    </queries>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- F10-10: foreground service for the duration of a turn. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Requested at runtime on API 33+ by the JS side (WP-M5); the service
         must (and does) tolerate a denial. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Declared for WP-M5 (composer camera/mic); optional so devices/
         emulators without a camera or mic can still install the app, and so
         the manifest does not need touching again when those features land. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
</manifest>
